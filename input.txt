import std.io;
import io2;
import io3;

class Fac{

    int x;
    String y;
    int[] anArray;
    int foo(int anInt, int[] anArray, boolean aBool) {return a+2*3;}
    public int computeFac (int num){
        int num_aux ;
        int[] num_aux_array ;
        if ( num < 1)
            num_aux = 1;
        else
            num_aux = num * ( this.computeFac(num - 1));
        return num_aux ;
    }
    public static void main ( String[] args ){
        io.println (new Fac().computeFac(10));
    }
}