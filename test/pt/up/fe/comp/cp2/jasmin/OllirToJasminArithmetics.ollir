import utils;

MyClass{


    .construct MyClass().V {
        invokespecial(this, "<init>").V;
    }

    .method public static main(args.array.String).V {
        t0.i32 :=.i32 5.i32 *.i32 6.i32;
        x.i32 :=.i32 invokestatic(utils, "add", 4.i32, t0.i32).i32;
    }


}